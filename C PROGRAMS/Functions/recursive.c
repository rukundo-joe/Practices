#include <stdio.h>

int main(){
    int n, result;
    printf("Enter a positive number: ");
    scanf("%d", &n);
    for(int x = 0; x <= n; x++){
         printf("Natural numbers between 0 and %d are %d", n, x);

    }
    //result = natural(n);
    return 0;
}




